class User {
  String? first_name;
  String? last_name;
  String? username;
  String? password;
  String? email;

  User({this.first_name, this.last_name, this.username, this.password, this.email});
}